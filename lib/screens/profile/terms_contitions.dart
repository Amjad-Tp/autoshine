import 'package:flutter/material.dart';

class TermsContitions extends StatelessWidget {
  const TermsContitions({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Saved Address Screen')));
  }
}
